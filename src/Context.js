import { createContext } from "react";

export const SearchContext = createContext(null);
export const CharacterSelectMenuContext = createContext(null);
export const CharacterSelectSearchbar = createContext(null);